query Me {
  me {
    id
    username
    displayName
    createdAt
    updatedAt
  }
}

query User($id: ID!) {
  user(id: $id) {
    id
    username
    displayName
    createdAt
    updatedAt
  }
}
