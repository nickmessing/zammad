query Me {
  me {
    id
    username
    displayName
    createdAt
    updatedAt
  }
}

query User($userId: ID!) {
  user(id: $userId) {
    id
    username
    displayName
    createdAt
    updatedAt
  }
}
