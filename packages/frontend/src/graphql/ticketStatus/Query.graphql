query TicketStatuses {
  ticketStatuses {
    id
    order
    createdAt
    updatedAt
  }
}

query TicketStatusBase($ticketStatusId: ID!) {
  ticketStatus(id: $ticketStatusId) {
    id
    name
    createdAt
    updatedAt
    colorBase {
      hue
      saturation
    }
  }
}
